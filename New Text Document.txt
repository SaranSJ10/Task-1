1)
HTTP 1.1
*It works on the textual format.
*It loads resources one after the other, so if one resource cannot be loaded, it blocks all the other resources behind it.

HTTP 2
*It works on Binary protocal.
*It is able to use a single TCP connection to send multiple streams of data at once so that no one resource blocks any other resource.

2)
 *In JavaScript, objects are a fundamental data type used to store and manipulate data. 
 *Objects can represent various entities, such as real-world objects, data structures, functions, and more.
 *The internal representation of objects in JavaScript is based on key-value pairs, where the keys are strings (or Symbols) and the values can be of any data type, including other objects.